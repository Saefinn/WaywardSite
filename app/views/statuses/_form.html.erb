<script>
$(function() {
    // Replace all textarea's
    // with SCEditor
    $("textarea").sceditor({
        plugins: "bbcode",
	style: "jquery.sceditor.default.min.css",
    toolbar: "bold,italic,underline,strike|bulletlist,orderedlist,code,quote|image,link,unlink,maximize|left,center,right|color,cut,copy,paste,pastetext|source"
    });
});
</script>


<%= simple_form_for(@status, html: {class: "form-horizontal" }) do |f| %>
  <% if @status.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@status.errors.count, "error") %> prohibited this status from being saved:</h2>

      <ul>
      <% @status.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>


   
   <%= f.input :title, placeholder: 'Journal Title' %>
   
   <div class="post-box"><%= f.input :content, placeholder: 'Journal Content' %></div>
   
   
   </br>
    <%= f.submit "Post" %>
  
<% end %>
