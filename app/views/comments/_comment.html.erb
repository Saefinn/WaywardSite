<div class="thread">
<strong><p><%= comment.user.profile_name%> says...</p></strong>
<p class="my-pre"><%= comment.comment.bbcode_to_html.html_safe  %></p>




  <% if current_user.present? %>
      <% if comment.user.profile_name == current_user.profile_name %>
        <span class="admin">
          
          <p><%= link_to "Edit", edit_post_comment_path(comment.post, comment) %> | <%= link_to "Delete", [comment.post, comment], method: :delete, data: { confirm: "Are you Sure?" } %></p>
        </span>
        <% else %>
          
       
      
      <% end %>
    
      <% if current_user.profile_name == "admin" %>
        <span class="admin">
          <%= link_to "Edit", edit_post_comment_path(comment.post, comment) %> | <%= link_to "Delete", [comment.post, comment], method: :delete, data: { confirm: "Are you Sure?" } %>
        </span>
        <% else %>
          
       
      
      <% end %>
    
  <% end %>
  
  </div>